<UserControl x:Class="HudiSoftPOS.Views.FinanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="#F4F6F8">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title + Actions -->
        <Border Grid.Row="0" Background="#1565C0" Padding="24,16" Margin="0,0,0,20">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Finance" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Financial overview: revenue, expenses, and profit tracking." FontSize="12" Foreground="#BBDEFB" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Command="{Binding DataContext.NavigateToReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            Content="View Reports" Background="White" Foreground="#1565C0" BorderThickness="0" Height="40" Padding="20,0" Margin="0,0,10,0" FontWeight="SemiBold" Cursor="Hand"/>
                    <Button Command="{Binding OpenAddTransactionCommand}" Content="Add Transaction" Background="#1A237E" Foreground="White" BorderThickness="0" Height="40" Padding="20,0" FontWeight="SemiBold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Cards (green / red / blue borders) -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <Border Background="White" Padding="24" Margin="0,0,8,0" BorderBrush="#4CAF50" BorderThickness="0,0,0,4" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding TotalIncome, StringFormat='${0:N2}'}" FontSize="28" FontWeight="Bold" Foreground="#4CAF50"/>
                    <TextBlock Text="Total Income" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="White" Padding="24" Margin="4,0" BorderBrush="#EF5350" BorderThickness="0,0,0,4" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding TotalExpense, StringFormat='${0:N2}'}" FontSize="28" FontWeight="Bold" Foreground="#EF5350"/>
                    <TextBlock Text="Total Expense" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="White" Padding="24" Margin="8,0,0,0" BorderBrush="#1565C0" BorderThickness="0,0,0,4" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="{Binding Balance, StringFormat='${0:N2}'}" FontSize="28" FontWeight="Bold" Foreground="#1565C0"/>
                    <TextBlock Text="Balance" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Filters -->
        <Border Grid.Row="2" Background="White" Padding="16" Margin="0,0,0,16" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <WrapPanel Orientation="Horizontal">
                <Border BorderBrush="#BDBDBD" BorderThickness="1" CornerRadius="4" Width="220" Height="36" Padding="8,0" Margin="0,0,12,8" VerticalAlignment="Center">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Magnify" Width="18" Height="18" Foreground="#757575" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="13"/>
                    </Grid>
                </Border>
                <StackPanel Orientation="Horizontal" Margin="0,0,12,8" VerticalAlignment="Center">
                    <TextBlock Text="From" FontSize="11" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker SelectedDate="{Binding FromDate}" Width="110" Height="36" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,12,8" VerticalAlignment="Center">
                    <TextBlock Text="To" FontSize="11" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker SelectedDate="{Binding ToDate}" Width="110" Height="36" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,12,8" VerticalAlignment="Center">
                    <TextBlock Text="Type" FontSize="11" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ComboBox ItemsSource="{Binding TransactionTypes}" SelectedItem="{Binding SelectedType}" Width="100" Height="36" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                </StackPanel>
                <Button Command="{Binding ApplyFiltersCommand}" Content="Apply Filters" Background="#1565C0" Foreground="White" BorderThickness="0" Height="36" Padding="16,0" Margin="0,0,8,8" Cursor="Hand"/>
                <Button Command="{Binding ExportCSVCommand}" Content="Export CSV" Background="#4CAF50" Foreground="White" BorderThickness="0" Height="36" Padding="16,0" Margin="0,0,8,8" Cursor="Hand"/>
                <Button Command="{Binding OpenAddTransactionCommand}" Content="Add Transaction" Background="#7E57C2" Foreground="White" BorderThickness="0" Height="36" Padding="16,0" Margin="0,0,0,8" Cursor="Hand"/>
            </WrapPanel>
        </Border>

        <!-- Transaction Table -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" BorderThickness="0" CanUserAddRows="False"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                  HeadersVisibility="Column" FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DATE" Binding="{Binding Date, StringFormat='dd/MM/yyyy'}" Width="120"/>
                <DataGridTemplateColumn Header="TYPE" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Income">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="Expense">
                                                <Setter Property="Background" Value="#FFEBEE"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Income">
                                                    <Setter Property="Text" Value="income"/>
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Expense">
                                                    <Setter Property="Text" Value="expense"/>
                                                    <Setter Property="Foreground" Value="#C62828"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="AMOUNT" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Amount, StringFormat=`${0:N2}}" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Income">
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Type}" Value="Expense">
                                                <Setter Property="Foreground" Value="#C62828"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="PAYMENT" Binding="{Binding PaymentMethod}" Width="100"/>
                <DataGridTextColumn Header="NOTE" Binding="{Binding Note}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add Transaction Dialog -->
        <Grid Grid.RowSpan="4" Background="#66000000" Visibility="{Binding IsAddTransactionOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White" Width="500" MaxHeight="600" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="8" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="New Transaction" FontSize="20" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,20"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Date" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <DatePicker SelectedDate="{Binding NewDate}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                            <TextBlock Text="Type" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding AddTransactionTypes}" SelectedItem="{Binding NewType}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                            <TextBlock Text="Amount ($)" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding NewAmount}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                            <TextBlock Text="Category" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding NewCategory}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                            <TextBlock Text="Payment" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding NewPaymentMethod}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                            <TextBlock Text="Note" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding NewNote}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        </StackPanel>
                    </ScrollViewer>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding CloseAddTransactionCommand}" Content="Cancel" Width="100" Height="40" Margin="0,0,10,0" Background="#E0E0E0" Foreground="#212121"/>
                        <Button Command="{Binding SaveTransactionCommand}" Content="Save" Width="100" Height="40" Background="#1565C0" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
