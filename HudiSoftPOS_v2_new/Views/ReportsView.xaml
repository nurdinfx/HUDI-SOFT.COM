<UserControl x:Class="HudiSoftPOS.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1600"
             Background="#F4F6F8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Reports Dashboard Banner -->
        <Border Grid.Row="0" Background="#1565C0" Padding="24,20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Reports Dashboard" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="View and export financial and operational reports" FontSize="13" Foreground="#BBDEFB" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Report Tiles Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,0">
            <ItemsControl ItemsSource="{Binding ReportTiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" Rows="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8" Padding="20" Margin="8" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                            <Button Command="{Binding DataContext.OpenReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Padding="0" Cursor="Hand">
                                <StackPanel>
                                    <materialDesign:PackIcon Kind="ChartBar" Width="40" Height="40" Foreground="#1565C0" Margin="0,0,0,12"/>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="#212121" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Button>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
