<UserControl x:Class="HudiSoftPOS.Views.PurchaseDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="#F4F6F8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Purchase Management Banner -->
        <Border Grid.Row="0" Background="#1565C0" Padding="24,20" Margin="0,0,0,20">
            <TextBlock Text="Purchase Management" FontSize="28" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Main content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,0">
            <StackPanel>
                <!-- Summary Cards -->
                <UniformGrid Columns="3" Margin="0,0,0,24">
                    <Border Background="White" CornerRadius="8" Padding="24" Margin="0,0,12,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding TotalPurchaseAmount, StringFormat='${0:N2}'}" FontSize="28" FontWeight="Bold" Foreground="#1565C0"/>
                                <TextBlock Text="Total Purchase Amount" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding TotalPurchaseAmountToday, StringFormat='Today: ${0:N2}'}" FontSize="11" Foreground="#9E9E9E" Margin="0,2,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Grid.Column="1" Kind="Receipt" Width="40" Height="40" Foreground="#1565C0" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="24" Margin="6,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding TotalPurchases}" FontSize="28" FontWeight="Bold" Foreground="#1565C0"/>
                                <TextBlock Text="Total Purchases" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Grid.Column="1" Kind="PackageVariant" Width="40" Height="40" Foreground="#1565C0" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="24" Margin="12,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding ActiveSuppliers}" FontSize="28" FontWeight="Bold" Foreground="#1565C0"/>
                                <TextBlock Text="Active Suppliers" FontSize="12" Foreground="#757575" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding PendingOrdersCount, StringFormat='Pending Orders: {0}'}" FontSize="11" Foreground="#9E9E9E" Margin="0,2,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Grid.Column="1" Kind="Domain" Width="40" Height="40" Foreground="#1565C0" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </UniformGrid>

                <!-- Purchase Modules -->
                <TextBlock Text="Purchase Modules" FontSize="18" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                <UniformGrid Columns="5" Rows="1" Margin="0,0,0,24">
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,8,8" Cursor="Hand" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.NavigateToPurchaseProductsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </Border.InputBindings>
                        <Button Command="{Binding DataContext.NavigateToPurchaseProductsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0" Cursor="Hand">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="Cart" Width="48" Height="48" Foreground="#1565C0" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                                <TextBlock Text="Purchase Products" FontWeight="Bold" FontSize="14" Foreground="#212121" HorizontalAlignment="Left"/>
                                <TextBlock Text="Create new purchases and manage inventory" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                <TextBlock Text="Open module →" FontSize="11" Foreground="#1565C0" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="4,0,4,8" Cursor="Hand" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.NavigateToOrdersCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </Border.InputBindings>
                        <Button Command="{Binding DataContext.NavigateToOrdersCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0" Cursor="Hand">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="ClipboardList" Width="48" Height="48" Foreground="#1565C0" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                                <TextBlock Text="Purchase Orders" FontWeight="Bold" FontSize="14" Foreground="#212121" HorizontalAlignment="Left"/>
                                <TextBlock Text="Manage purchase orders and approvals" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                <TextBlock Text="Open module →" FontSize="11" Foreground="#1565C0" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="4,0,4,8" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="Domain" Width="48" Height="48" Foreground="#1565C0" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                            <TextBlock Text="Suppliers" FontWeight="Bold" FontSize="14" Foreground="#212121" HorizontalAlignment="Left"/>
                            <TextBlock Text="Manage supplier information and contacts" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,8"/>
                            <TextBlock Text="Open module →" FontSize="11" Foreground="#1565C0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="4,0,4,8" Cursor="Hand" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.NavigateToReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </Border.InputBindings>
                        <Button Command="{Binding DataContext.NavigateToReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0" Cursor="Hand">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="ChartBar" Width="48" Height="48" Foreground="#1565C0" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                                <TextBlock Text="Product Report" FontWeight="Bold" FontSize="14" Foreground="#212121" HorizontalAlignment="Left"/>
                                <TextBlock Text="View purchase analytics and reports" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                <TextBlock Text="Open module →" FontSize="11" Foreground="#1565C0" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="8,0,0,8" Cursor="Hand" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.NavigateToReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </Border.InputBindings>
                        <Button Command="{Binding DataContext.NavigateToReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0" Cursor="Hand">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="CalendarClock" Width="48" Height="48" Foreground="#1565C0" HorizontalAlignment="Left" Margin="0,0,0,12"/>
                                <TextBlock Text="Daily Purchase Transactions" FontWeight="Bold" FontSize="14" Foreground="#212121" HorizontalAlignment="Left"/>
                                <TextBlock Text="View daily purchase history and transactions" FontSize="12" Foreground="#757575" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                <TextBlock Text="Open module →" FontSize="11" Foreground="#1565C0" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Button>
                    </Border>
                </UniformGrid>

                <!-- System Status -->
                <TextBlock Text="System Status" FontSize="18" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <UniformGrid Columns="3" Rows="1">
                        <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,8,0" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="5" FontSize="24" FontWeight="Bold" Foreground="#1565C0"/>
                                <TextBlock Text="Available Modules" FontSize="12" Foreground="#757575"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="20" Margin="4,0" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Database" Width="32" Height="32" Foreground="#1565C0" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock Text="Real Purchase Data" FontSize="14" FontWeight="SemiBold" Foreground="#212121" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="20" Margin="8,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Server" Width="32" Height="32" Foreground="#1565C0" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock Text="Live Backend" FontSize="14" FontWeight="SemiBold" Foreground="#212121" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentTime}" FontSize="12" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <Border Background="#4CAF50" CornerRadius="4" Padding="8,4">
                            <TextBlock Text="{Binding ConnectionStatus}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding DataContext.CurrentUserFullName, RelativeSource={RelativeSource AncestorType=Window}, StringFormat='User: {0}'}" FontSize="11" Foreground="#757575" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding BackendUrl, StringFormat='Backend: {0}'}" FontSize="11" Foreground="#757575" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding DataContext.CurrentDate, RelativeSource={RelativeSource AncestorType=Window}, StringFormat='Date: {0}'}" FontSize="11" Foreground="#757575" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
