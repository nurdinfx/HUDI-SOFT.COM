<UserControl x:Class="HudiSoftPOS.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F4F6F8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1565C0" Padding="24,16" Margin="0,0,0,0">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="System Settings" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Configure your restaurant system preferences" FontSize="12" Foreground="#BBDEFB" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Command="{Binding SaveSettingsCommand}" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" FontSize="14">
                    <TextBlock Text="Save Settings" TextDecorations="Underline"/>
                </Button>
            </Grid>
        </Border>

        <!-- Tabs -->
        <TabControl Grid.Row="1" Margin="20,20,20,0" Background="White" BorderThickness="0" Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="20,12"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="Restaurant Information" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                        <TextBlock Text="Restaurant Name *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding RestaurantName}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Email Address *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding EmailAddress}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Address *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Address}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Website" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Website}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Phone Number *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding PhoneNumber}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Tax ID" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding TaxId}" Margin="0,0,0,24" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="POS Settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="POS Configuration" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                        <TextBlock Text="Tax Rate (%) *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding TaxRatePercent}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Service Charge (%) *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ServiceChargePercent}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Currency *" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding CurrencyList}" SelectedItem="{Binding Currency}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        <TextBlock Text="Receipt Print Size" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding ReceiptSizeList}" SelectedItem="{Binding ReceiptPrintSize}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        <TextBlock Text="Receipt Header" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ReceiptHeader}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBlock Text="Receipt Footer" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ReceiptFooter}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,24" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Business Hours">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="Business Hours" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                        <TextBlock Text="{Binding BusinessHoursNote}" FontSize="13" Foreground="#757575" TextWrapping="Wrap" Margin="0,0,0,24"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Network">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="API / Backend" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                        <TextBlock Text="Base URL" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ApiBaseUrl}" Margin="0,0,0,24" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="System">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="Business Accounts" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,16,0">
                                <TextBlock Text="ZAAD Account Number" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding ZaadAccountNumber}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <TextBlock Text="SAHAL Account Number" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding SahalAccountNumber}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="E-DAHAB Account Number" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding EDahabAccountNumber}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <TextBlock Text="MyCash Account Number" FontSize="12" Foreground="#757575" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding MyCashAccountNumber}" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="Maintenance" FontSize="16" FontWeight="Bold" Foreground="#212121" Margin="0,24,0,16"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding ResetToDefaultsCommand}" Content="Reset to Defaults" Background="#E0E0E0" Foreground="#212121" BorderThickness="0" Height="40" Padding="20,0" Margin="0,0,12,0" Cursor="Hand"/>
                            <Button Command="{Binding ClearAllCacheCommand}" Content="Clear All Cache" Background="#EF5350" Foreground="White" BorderThickness="0" Height="40" Padding="20,0" Margin="0,0,12,0" Cursor="Hand"/>
                            <Button Command="{Binding DataContext.NavigateToLicenseCommand, RelativeSource={RelativeSource AncestorType=Window}}" Content="License Management" Background="#4CAF50" Foreground="White" BorderThickness="0" Height="40" Padding="20,0" Cursor="Hand"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
