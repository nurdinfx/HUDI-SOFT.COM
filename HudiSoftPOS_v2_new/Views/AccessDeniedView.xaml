<UserControl x:Class="HudiSoftPOS.Views.AccessDeniedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid Background="#F4F6F8">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Icon -->
            <Border Width="120" Height="120" Background="#FEE2E2" CornerRadius="60" Margin="0,0,0,40">
                <materialDesign:PackIcon Kind="ShieldLock" Width="60" Height="60" Foreground="#B91C1C" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Text -->
            <TextBlock Text="ACCESS DENIED" FontSize="48" FontWeight="Black" Foreground="#1A1A1A" HorizontalAlignment="Center" Margin="0,0,0,15"/>
            <TextBlock Text="{Binding Message}" FontSize="20" Foreground="#616161" HorizontalAlignment="Center" Margin="0,0,0,50" TextAlignment="Center" MaxWidth="600" TextWrapping="Wrap"/>

            <!-- Buttons -->
            <Button Command="{Binding GoBackCommand}" 
                    Content="Return to POS" 
                    Height="60" Padding="40,0"
                    FontSize="18" FontWeight="Bold"
                    Background="#1B60F5" Foreground="White" BorderThickness="0"
                    materialDesign:ButtonAssist.CornerRadius="12"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
